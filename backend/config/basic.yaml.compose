# 服务器配置
server:
  port: 8080                    # 服务端口
  mode: debug                   # 运行模式: debug/release/test
  read_timeout: 30s             # 读取超时
  write_timeout: 30s            # 写入超时
  max_header_bytes: 1048576     # 最大请求头大小 (1MB)

# API配置
api:
  protocol: https               # 协议: http/https
  host: api.example.com         # API主机
  port: 443                     # API端口
  version: v1                   # API版本
  prefix: /api                  # API路径前缀

# JWT配置
jwt:
  secret: "your-super-secret-jwt-key-change-in-production"  # JWT密钥
  expire: 24h                    # 访问令牌过期时间
  refresh_expire: 168h           # 刷新令牌过期时间 (7天)
  issuer: "tiny-forum"          # 签发者

# 日志配置
log:
  level: info                    # 日志级别: debug/info/warn/error
  filename: ./logs/app.log       # 日志文件路径
  max_size: 100                  # 单个日志文件最大大小(MB)
  max_backups: 10                # 保留的旧日志文件最大数量
  max_age: 30                    # 保留日志文件的最大天数
  compress: true                 # 是否压缩旧日志文件
  console: true                  # 是否输出到控制台
  json_format: false             # 是否使用JSON格式

# 文件上传配置
upload:
  max_size: 5242880             # 最大文件大小(5MB)
  allowed_types:                # 允许的文件类型
    - image/jpeg
    - image/png
    - image/gif
    - image/webp
  storage_path: ./uploads       # 存储路径
  cdn_domain: https://cdn.example.com  # CDN域名
  enable_compress: true         # 是否启用压缩

# 限流配置
rate_limit:
  enabled: true                 # 是否启用
  requests: 100                 # 请求次数
  duration: 60                  # 时间窗口(秒)
  burst: 50                     # 突发请求数

ollama:
  base_url: http://192.168.5.243:11434
  model: qwen3:0.6b
  num_predict: 256 # 限制生成的最大 token 数量
  temperature: 0.7 # 适当降低随机性
  timeout: 60 # 超时时间

# 根据你自己的情况修改 IP

allow_origins:
  - http://localhost:3000
  - http://127.0.0.1:3000
  - http://192.168.5.243:8080
  - http://192.168.5.180:8080
