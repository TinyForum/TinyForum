# 脱敏策略

1. 中文姓名脱敏：保留第一个字 `张三 -> 张*`
2. 手机号：保留前3后4 `138****1234`
3. 邮箱：保留第一个字符和@域名 `a****@b.com`
4. 身份证：保留前6后4，中间隐藏 `123456********4321`
5. 银行卡：保留前6后4，中间隐藏 `123456********4321`
6. 完全隐藏：全部隐藏 `*********`
7. 自定义: 使用 `regex` 定义正则表达式，例如 `regex:^1[3456789]\d{9}$`
