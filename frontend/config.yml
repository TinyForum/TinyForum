# Frontend Configuration (Next.js)
# Generated by TinyForum setup script
# DO NOT EDIT MANUALLY — run 'make init-dev' to regenerate

output: standalone

images:
  dangerously_allow_svg: true
  remote_patterns:
    - protocol: https
      hostname: api.dicebear.com
    - protocol: https
      hostname: images.unsplash.com
    - protocol: http
      hostname: localhost

allowed_dev_origins:
  - "192.168.0.116"
  - "127.0.0.1"
  - "localhost"

experimental:
  proxy_timeout: 10000

proxy:
  enabled: true
  backend_url: "http://192.168.0.116:8080"
  source: "/api/v1/:path*"
  destination_pattern: "/api/v1/:path*"
